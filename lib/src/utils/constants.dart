// lib\src\utils\constants.dart

import 'package:flutter/material.dart';

const Gradient kMaskingGradient = LinearGradient(
  colors: [Colors.transparent, Colors.black],
  stops: [0.0, 0.5],
  begin: Alignment.topCenter,
  end: Alignment.bottomCenter,
);
